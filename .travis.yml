sudo: required

services:
  - docker

#branches:
#  only:
#    - master
  
script:
  - make image $TRAVIS_BRANCH
  - make test $TRAVIS_BRANCH

after_success:
  - docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD ;
  - make push-image ;

notifications:
  email: false
