sudo: required

services:
  - docker

branches:
  only:
    - master
  
script:
  - make image
  - make test

after_success:
  - docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD ;
  - make push-image ;
#  - if [[ "$TRAVIS_BRANCH" == "master" ]]; then
#      docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD ;
#      make push-image ;
#    fi

notifications:
  email: false
